# Toyota 素材目录

固定分类：`exterior/`、`interior/`、`driving/`、`factory/`、`launch/`、`logo/`。市场版本、车型年款和来源地区必须进入 `vehicle`、`tags` 与授权范围。

来源与用途限制记录在 `SOURCE_RECORD.md`。Toyota 官方新闻资料统一标记为 `official_press_asset`；该状态只表示受限新闻报道用途，不表示 unrestricted commercial use。派生审核文件放入 `previews/`，扫描原始素材时必须排除该目录。
